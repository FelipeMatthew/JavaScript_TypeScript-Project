import styled from 'styled-components';

import { primaryColor } from '../../config/colors';

export const ErrorTxt = styled.h1`
  font-size: 60px;
  text-align: center;

  span {
    color: ${primaryColor};
  }
`;
